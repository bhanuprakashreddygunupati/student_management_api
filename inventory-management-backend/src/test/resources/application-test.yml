server:
  servlet:
    context-path: ""

spring:
  datasource:
    url: jdbc:h2:mem:testdb;MODE=MySQL;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    username: sa
    password:
    driver-class-name: org.h2.Driver

  jpa:
    hibernate:
      ddl-auto: create-drop
    open-in-view: true   
    show-sql: false

jwt:
  secret: set real secret in env JWT_SECRET 
  expiration: 86400000
  refresh-expiration: 604800000